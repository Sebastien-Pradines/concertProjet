{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1 class="title">{{ band.name }}</h1>

    <h2 class="title">Membres:</h2>
    <div class="container-fluid row example-wrapper">

        {%  for member in band.members %}
            <div class="card" style="width: 18rem; margin: 2px;">
                <img src="{{ asset('uploads/pictures/' ~ member.picture) }}" class="card-img-top" height="400px">
                <div class="card-body">
                    <h5 class="card-title">{{ member.firstName }} {{ member.lastName }}</h5>
                    <p class="card-text"> Rôle: {{ member.groupRole }}</p>
                    <p class="card-text"> Date de naissance: {{ member.birthDate|date("d/m/Y") }}</p>
                </div>
            </div>
        {%  endfor %}
    </div>
    <hr class="hr" />
    <h2 class="title">Concerts:</h2>
    <div class="container-fluid row example-wrapper">

        {%  for concert in band.concerts %}
            <div class="card" style="width: 18rem; margin: 2px;">
                <div class="card-body">
                    <h5 class="card-title">{{ concert.name }}</h5>
                    <p class="card-text"> Salle: {{ concert.stage.name }}</p>
                    <p class="card-text"> Date: {{ concert.date|date("d/m/Y") }}</p>
                </div>
            </div>
        {%  endfor %}
    </div>

{% endblock %}
