{% extends 'base.html.twig' %}

{% block title %}New Member{% endblock %}

{% block body %}
    <h1 class="title">Create new Member</h1>

    <div class="container-fluid example-wrapper">
        {{ include('member/_form.html.twig') }}
    </div>

    <div class="container-fluid bottom-wrapper">
        <div class="row crud-button-div">
            <button class="btn crud-button">
                <a class="button-link" href="{{ path('app_member_index') }}">Retour</a>
            </button>
        </div>
    </div>
{% endblock %}
